# Spotify Client ID lives in config.js. With PKCE there is no client secret,
# so the Client ID is committed in plain text (it is sent in the auth URL anyway).
# Security relies on the redirect URI whitelist in the Spotify developer dashboard.

# macOS noise
.DS_Store
.AppleDouble
.LSOverride
Icon?

# Editors
.vscode/
.idea/
*.swp
*~
.zed/

# Node (in case we add a build step later)
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Logs + tmp
*.log
tmp/
